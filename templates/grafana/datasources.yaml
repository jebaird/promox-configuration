# Grafana Datasource Provisioning
# Auto-configures Prometheus as default datasource

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: {{PROMETHEUS_URL}}
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
      httpMethod: "POST"
