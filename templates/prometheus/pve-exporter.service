[Unit]
Description=Prometheus PVE Exporter
After=network.target

[Service]
Type=simple
User=prometheus
ExecStart=/usr/bin/pve_exporter --config.file /etc/prometheus/pve.yml --web.listen-address=:9221
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
